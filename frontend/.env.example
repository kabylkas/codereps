# Base URL of the backend API.
#
# Defaults to http://127.0.0.1:8000/api when unset. Use the IPv4 literal rather
# than "localhost": macOS resolves localhost to ::1 first, and Docker Desktop
# binds *:8000 on IPv6, which shadows the backend.
#
# VITE_API_URL=http://127.0.0.1:8000/api
